// src/server/models.ts

export interface Profile {
    id: string;
    name: string;
    email: string;
    // Add other fields as needed
  }
  
  export interface Tour {
    id: string;
    name: string;
    description: string;
    // Add other fields as needed
  }
  